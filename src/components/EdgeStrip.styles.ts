// Feature: UI | Why: EdgeStrip styles extracted for 100-line law compliance
import { StyleSheet } from 'react-native';

export const edgeStripStyles = StyleSheet.create({
    strip: { overflow: 'hidden' },
});
