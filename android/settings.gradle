rootProject.name = 'SentientAIBrowser'
include ':app'
// Ensure local Maven repo for React Native AAR
pluginManagement {
	repositories {
		maven {
			url "$rootDir/../node_modules/react-native/android"
		}
		google()
		mavenCentral()
		gradlePluginPortal()
	}
}
